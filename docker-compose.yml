version: "3.9"

services:

  photovault:

    build: .

    container_name: photovault

    ports:
      - "5000:5000"

    volumes:
      - ./config:/config
      - ./database:/database
      - /mnt/user/photos:/photos

    restart: unless-stopped
